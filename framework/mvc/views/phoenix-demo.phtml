<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PHOENIX Demo - The Mother of All Dashboard Builders</title>

    <!-- PHOENIX Core CSS -->
    <link rel="stylesheet" href="/phoenix/assets/css/phoenix-core.css">

    <!-- Widget Styles (inline for demo) -->
    <style>
        <?php
        // Include widget CSS files
        $widgets = ['stats-card', 'chart-bar', 'progress-bar', 'activity-feed', 'data-table'];
        foreach ($widgets as $widget) {
            $cssFile = dirname(dirname(__DIR__)) . "/phoenix/widgets/$widget/widget.css";
            if (file_exists($cssFile)) {
                echo "/* Widget: $widget */\n";
                echo file_get_contents($cssFile) . "\n\n";
            }
        }
        ?>

        /* Demo Page Styles */
        .phoenix-demo-header {
            text-align: center;
            padding: 60px 30px 40px;
            position: relative;
            z-index: 1;
        }

        .phoenix-demo-logo {
            font-size: 4rem;
            margin-bottom: 20px;
            display: block;
        }

        .phoenix-demo-title {
            font-size: 3rem;
            font-weight: 800;
            margin: 0 0 10px 0;
            background: linear-gradient(135deg, #00d4ff, #7b2cbf, #ff006e);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .phoenix-demo-subtitle {
            font-size: 1.2rem;
            color: var(--phoenix-text-secondary);
            margin: 0;
        }

        .demo-section {
            margin-bottom: var(--phoenix-gap-lg);
        }

        .demo-section-title {
            font-size: 1.3rem;
            font-weight: 600;
            margin: 0 0 20px 0;
            color: var(--phoenix-text-primary);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .demo-section-title::before {
            content: '';
            width: 4px;
            height: 24px;
            background: linear-gradient(180deg, var(--phoenix-accent-primary), var(--phoenix-accent-secondary));
            border-radius: 2px;
        }

        .demo-container {
            max-width: 1600px;
            margin: 0 auto;
            padding: 0 30px 60px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: var(--phoenix-gap);
        }

        .charts-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: var(--phoenix-gap);
        }

        .progress-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: var(--phoenix-gap);
        }

        .theme-switcher {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            display: flex;
            gap: 8px;
        }

        .theme-btn {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            border: 2px solid var(--phoenix-border-color);
            cursor: pointer;
            transition: all 0.2s ease;
        }

        .theme-btn:hover {
            transform: scale(1.1);
            border-color: var(--phoenix-accent-primary);
        }

        .theme-btn.active {
            border-color: var(--phoenix-accent-primary);
            box-shadow: 0 0 10px var(--phoenix-glow-primary);
        }

        .theme-btn[data-theme="dark"] { background: #0a0a0f; }
        .theme-btn[data-theme="cyber"] { background: linear-gradient(135deg, #00d4ff, #7b2cbf); }
        .theme-btn[data-theme="matrix"] { background: linear-gradient(135deg, #00ff00, #003300); }
        .theme-btn[data-theme="sunset"] { background: linear-gradient(135deg, #ff6b35, #f7931e); }
        .theme-btn[data-theme="ocean"] { background: linear-gradient(135deg, #0077b6, #00b4d8); }

        /* Particles */
        .particles-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        @media (max-width: 1200px) {
            .stats-grid { grid-template-columns: repeat(2, 1fr); }
            .charts-grid { grid-template-columns: 1fr; }
        }

        @media (max-width: 768px) {
            .stats-grid { grid-template-columns: 1fr; }
            .progress-grid { grid-template-columns: 1fr; }
            .phoenix-demo-title { font-size: 2rem; }
        }
    </style>
</head>
<body class="phoenix-page theme-dark">

    <!-- Particles Background -->
    <div class="particles-container" id="particles"></div>

    <!-- Theme Switcher -->
    <div class="theme-switcher">
        <button class="theme-btn active" data-theme="dark" title="Dark"></button>
        <button class="theme-btn" data-theme="cyber" title="Cyber"></button>
        <button class="theme-btn" data-theme="matrix" title="Matrix"></button>
        <button class="theme-btn" data-theme="sunset" title="Sunset"></button>
        <button class="theme-btn" data-theme="ocean" title="Ocean"></button>
    </div>

    <!-- Header -->
    <header class="phoenix-demo-header">
        <span class="phoenix-demo-logo">🔥</span>
        <h1 class="phoenix-demo-title"><?= htmlspecialchars($data['title']) ?></h1>
        <p class="phoenix-demo-subtitle"><?= htmlspecialchars($data['subtitle']) ?></p>
    </header>

    <!-- Main Content -->
    <main class="demo-container">

        <!-- Stats Section -->
        <section class="demo-section">
            <h2 class="demo-section-title">Stats Cards</h2>
            <div class="stats-grid">
                <?php foreach ($data['stats'] as $i => $stat): ?>
                <?php include dirname(dirname(__DIR__)) . '/phoenix/widgets/stats-card/widget.phtml';
                      // Variables for widget
                      $config = $stat;
                      $widget_id = 'stats-card-' . $i;
                ?>
                <?php endforeach; ?>
            </div>
        </section>

        <!-- Progress Bars Section -->
        <section class="demo-section">
            <h2 class="demo-section-title">Progress Bars</h2>
            <div class="progress-grid">
                <?php foreach ($data['progress'] as $i => $progress): ?>
                <?php
                    $config = $progress;
                    $widget_id = 'progress-bar-' . $i;
                    include dirname(dirname(__DIR__)) . '/phoenix/widgets/progress-bar/widget.phtml';
                ?>
                <?php endforeach; ?>
            </div>
        </section>

        <!-- Charts & Activity Section -->
        <section class="demo-section">
            <h2 class="demo-section-title">Charts & Activity</h2>
            <div class="charts-grid">
                <!-- Chart -->
                <div>
                    <?php
                        $config = [
                            'title' => 'Sales Performance',
                            'labels' => $data['chart']['labels'],
                            'datasets' => $data['chart']['datasets'],
                            'height' => 300
                        ];
                        $widget_id = 'chart-bar-1';
                        include dirname(dirname(__DIR__)) . '/phoenix/widgets/chart-bar/widget.phtml';
                    ?>
                </div>

                <!-- Activity Feed -->
                <div>
                    <?php
                        $config = [
                            'title' => 'Recent Activity',
                            'items' => $data['activities'],
                            'maxItems' => 5
                        ];
                        $widget_id = 'activity-feed-1';
                        include dirname(dirname(__DIR__)) . '/phoenix/widgets/activity-feed/widget.phtml';
                    ?>
                </div>
            </div>
        </section>

        <!-- Data Table Section -->
        <section class="demo-section">
            <h2 class="demo-section-title">Data Table</h2>
            <?php
                $config = [
                    'title' => 'Top Customers',
                    'columns' => $data['table']['columns'],
                    'data' => $data['table']['data'],
                    'searchable' => true,
                    'sortable' => true,
                    'pagination' => ['enabled' => true, 'perPage' => 5],
                    'actions' => [
                        ['label' => 'Export', 'icon' => '📥', 'action' => 'export']
                    ]
                ];
                $widget_id = 'data-table-1';
                include dirname(dirname(__DIR__)) . '/phoenix/widgets/data-table/widget.phtml';
            ?>
        </section>

    </main>

    <!-- Footer -->
    <footer style="text-align: center; padding: 40px; color: var(--phoenix-text-muted);">
        <p>Built with PHOENIX Engine • Powered by micro-MVC</p>
        <p style="margin-top: 10px; font-size: 0.9rem;">
            🤖 AI-powered dashboard building with MCP
        </p>
    </footer>

    <!-- PHOENIX Core JS -->
    <script src="/phoenix/assets/js/phoenix-core.js"></script>

    <!-- Widget Scripts -->
    <script>
        <?php
        // Include widget JS files
        foreach ($widgets as $widget) {
            $jsFile = dirname(dirname(__DIR__)) . "/phoenix/widgets/$widget/widget.js";
            if (file_exists($jsFile)) {
                echo "// Widget: $widget\n";
                echo file_get_contents($jsFile) . "\n\n";
            }
        }
        ?>
    </script>

    <!-- Demo Scripts -->
    <script>
        // Theme Switcher
        document.querySelectorAll('.theme-btn').forEach(btn => {
            btn.addEventListener('click', () => {
                const theme = btn.dataset.theme;
                document.body.className = 'phoenix-page theme-' + theme;

                document.querySelectorAll('.theme-btn').forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
            });
        });

        // Particles
        (function() {
            const container = document.getElementById('particles');
            const particleCount = 50;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.style.cssText = `
                    position: absolute;
                    width: ${Math.random() * 4 + 1}px;
                    height: ${Math.random() * 4 + 1}px;
                    background: var(--phoenix-accent-primary);
                    border-radius: 50%;
                    left: ${Math.random() * 100}%;
                    top: ${Math.random() * 100}%;
                    opacity: ${Math.random() * 0.5 + 0.1};
                    animation: float ${Math.random() * 10 + 10}s linear infinite;
                    animation-delay: -${Math.random() * 10}s;
                `;
                container.appendChild(particle);
            }

            const style = document.createElement('style');
            style.textContent = `
                @keyframes float {
                    0%, 100% { transform: translateY(0) translateX(0); }
                    25% { transform: translateY(-30px) translateX(10px); }
                    50% { transform: translateY(-60px) translateX(-10px); }
                    75% { transform: translateY(-30px) translateX(20px); }
                }
            `;
            document.head.appendChild(style);
        })();

        console.log('🔥 PHOENIX Demo loaded!');
    </script>

</body>
</html>
